config NFC_ST21NFC
	tristate
	---help---
	  STMicroelectronics ST21NFC core driver. It implements the chipset
	  HCI logic and hooks into the NFC kernel APIs. Physical layers will
	  register against it.
